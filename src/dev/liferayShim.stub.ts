// Production stub — replaces liferayShim.ts in `vite build` so dev-only code
// never ships in the Liferay custom element bundle.

export function installDevLiferayShim() {
  // no-op
}

installDevLiferayShim();
